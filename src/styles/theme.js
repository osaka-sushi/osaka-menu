import { extendTheme } from "@chakra-ui/react"

const theme = extendTheme({
    colors: {
        secondary: "#101010",
        primary: "#FF003D",
        tertiary: "#242424",
        text: "#fff",
    },
})

export default theme